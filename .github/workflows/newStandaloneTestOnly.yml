name: CI - New Standalone Test

on:
    push:
        paths:
            - 'BalanceITUnityGame/Assets/**'
            - 'BalanceITUnityGame/Packages/**'
            - 'BalanceITUnityGame/ProjectSettings/**'
    workflow_dispatch: {}
jobs:
    #Test123
    Test123:
        name: Test123
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo "Run ls command..."
                ls ${{ github.workspace }}
    # Testrunner Step
    testrunner:
        name: Testrun
        runs-on: ubuntu-latest
        steps:
            #LS CMD
            - run: |
                echo "Run ls command..."
                ls ${{ github.workspace }}
            #StandaloneTest Runnen mit GameCI
            - name: Run StandaloneTest
              id: standalone
              uses: game-ci/unity-test-runner@v4
              env:
                UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
                UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
                UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
              with:
                projectPath: ${{ github.workspace }}/BalanceITUnityGame/
                githubToken: ${{ secrets.CIGITHUB_TOKEN }}
                testmode: standalone
                artifactsPath: standalone-artifacts
            #TestResults uploaden
            - name: Upload Test Result
              uses: actions/upload-artifact@v4
              if: always()
              with:
                name: Test results for StandaloneTest
                path: standalone-artifacts
    # Build Step
    buildProject:
        name: ${{ matrix.target-platform  }}
        runs-on: ${{  matrix.os  }}
        needs: testrunner
        if: success()
        steps:
            #LS CMD
            - run: |
                echo "Run ls command..."
                ls ${{ github.workspace }}/BalanceITUnityGame
            #Repo Checkout
            - uses: actions/checkout@v4
            #Library Cache
            - uses: actions/cache@v4
              with:
                path: ${{ github.workspace }}/BalanceITUnityGame/Library
                key: Library-${{  matrix.unity-version  }}-${{  matrix.target-platform  }}
            #Builder Step
            - uses: game-ci/unity-builder@v4
              env:
                UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
                UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
                UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
                with:
                targetPlatform: ${{  matrix.target-platform  }}
                projectPath: ${{ github.workspace }}/BalanceITUnityGame
                unityVersion: ${{  matrix.unity-version  }}
                buildName: BalanceITUnityGameBuild
            #Artifactupload
            - uses: actions/upload-artifact@v4
              with:
                name: ${{  matrix.target-platform  }}
                path: build/${{  matrix.target-platform  }}
        strategy:
            matrix:
                unity-version:
                    - 2023.2.20f1
                    - auto
                os:
                    - ubuntu-latest
                    - windows-latest
                include:
                    - os: ubuntu-latest
                      target-platform: StandaloneLinux64
                    - os: windows-latest
                      target-platform: StandaloneWindows64